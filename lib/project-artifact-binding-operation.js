import { createProjectArtifactBinding } from './project-artifact-binding.js';
function fail(code,message,details=null){const error=new Error(message);error.code=code;error.details=details;throw error;}
function exactRevision(value){const revision=typeof value==='string'?value.trim().toLowerCase():'';if(!/^[0-9a-f]{40}$/.test(revision))fail('PROJECT_ARTIFACT_BINDING_INVALID','expected_revision must be an exact Git revision');return revision;}
function providerRequest(input){const repository=typeof input?.repository==='string'?input.repository.trim():'';const kind=input?.kind;const id=Number(input?.id);if(!/^[A-Za-z0-9_.-]+\/[A-Za-z0-9_.-]+$/.test(repository)||!['issue','pull_request'].includes(kind)||!Number.isInteger(id)||id<1)fail('PROJECT_ARTIFACT_BINDING_INVALID','provider identity is invalid');return {repository,kind,id};}
export function createProjectArtifactBindingOperation(dependencies){if(typeof dependencies?.inspectProject!=='function'||typeof dependencies?.readProviderArtifact!=='function'||typeof dependencies?.persistBinding!=='function')fail('PROJECT_ARTIFACT_BINDING_UNAVAILABLE','project artifact binding dependencies are unavailable');return Object.freeze({async bind(input){const expected=exactRevision(input?.expected_revision);const first=await dependencies.inspectProject({project_ref:input?.project_ref});if(String(first?.authority_revision||'').toLowerCase()!==expected)fail('PROJECT_ARTIFACT_BINDING_AUTHORITY_STALE','project authority moved before provider observation',{expected_revision:expected,actual_revision:first?.authority_revision||null});const provider=providerRequest(input?.provider);const observed=await dependencies.readProviderArtifact(provider);if(!observed||observed.repository!==provider.repository||observed.kind!==provider.kind||Number(observed.id)!==provider.id)fail('PROJECT_ARTIFACT_BINDING_PROVIDER_MISMATCH','provider readback did not match requested identity');const second=await dependencies.inspectProject({project_ref:input?.project_ref});if(String(second?.authority_revision||'').toLowerCase()!==expected)fail('PROJECT_ARTIFACT_BINDING_AUTHORITY_STALE','project authority moved before binding persistence',{expected_revision:expected,actual_revision:second?.authority_revision||null});const binding=createProjectArtifactBinding({project_ref:input.project_ref,transition_id:input.transition_id,authority_revision:expected,relationship:input.relationship,provider:observed,satisfaction:input.satisfaction});const persisted=await dependencies.persistBinding(binding);if(!persisted?.binding||JSON.stringify(persisted.binding)!==JSON.stringify(binding))fail('PROJECT_ARTIFACT_BINDING_PERSISTENCE_INDETERMINATE','durable binding readback did not match intended binding');return Object.freeze({ok:true,outcome:persisted.created?'bound':'already_bound',binding_id:persisted.binding_id,binding:persisted.binding});}});}