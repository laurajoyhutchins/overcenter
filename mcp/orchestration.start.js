import { executeCorrelatedCommand } from 'lib/orchestration-journal.js'; import { createPostgresOrchestrationRunService,statusForOrchestrationRunError } from 'lib/orchestration-runs.js'; export const access='admin'; export default {name:'orchestration.start',description:'Start or idempotently recover one durable orchestration run with a bounded budget and compatible predecessor pointer.',inputSchema:{type:'object',required:['run_id','worker','mode','continuation_key','scope'],properties:{run_id:{type:'string'},worker:{type:'string'},mode:{type:'string',enum:['scheduled','interactive']},continuation_key:{type:'string'},scope:{type:'object',required:['project'],properties:{project:{type:'string'},lanes:{type:'array',items:{type:'string'},maxItems:8},repositories:{type:'array',items:{type:'string'},maxItems:25},direction:{type:['string','null']}},additionalProperties:false},budget_seconds:{type:'integer'},settlement_reserve_seconds:{type:'integer'},minimum_new_gate_seconds:{type:'integer'}},additionalProperties:false},async handler(args,ctx){const r=await executeCorrelatedCommand('orchestration.start',args||{},input=>createPostgresOrchestrationRunService({db:ctx?.db}).start(input),{statusForFailure:statusForOrchestrationRunError,defaultError:'ORCHESTRATION_START_ERROR',defaultMessage:'orchestration.start failed',flattenDetails:true,db:ctx?.db});return r.body;}};