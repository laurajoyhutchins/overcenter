import { executeCorrelatedCommand } from 'lib/orchestration-journal.js'; import { createPostgresOrchestrationMaintenanceService,statusForOrchestrationRunError } from 'lib/orchestration-runs.js'; export const access='admin'; export default {name:'orchestration.maintain',description:'Perform only bounded deterministic recovery of expired/stuck coordination state and resolvable orchestration journal residue. It never selects, creates, prioritizes, or semantically edits portfolio work.',inputSchema:{type:'object',properties:{run_id:{type:'string'}},additionalProperties:false},async handler(args,ctx){const r=await executeCorrelatedCommand('orchestration.maintain',args||{},()=>createPostgresOrchestrationMaintenanceService({db:ctx?.db}).maintain(),{statusForFailure:statusForOrchestrationRunError,defaultError:'ORCHESTRATION_MAINTENANCE_ERROR',defaultMessage:'orchestration.maintain failed',flattenDetails:true,db:ctx?.db});return r.body;}};