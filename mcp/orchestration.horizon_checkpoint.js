import { executeCorrelatedCommand } from 'lib/orchestration-journal.js'; import { createPostgresOrchestrationRunService,statusForOrchestrationRunError } from 'lib/orchestration-runs.js'; export const access='admin'; export default {name:'orchestration.horizon_checkpoint',description:'Persist an agent-selected advisory execution horizon after mechanically validating current Linear execution fingerprints. This never claims work.',inputSchema:{type:'object',required:['run_id','candidates'],properties:{run_id:{type:'string'},candidates:{type:'array',minItems:1,maxItems:10,items:{type:'object',required:['work_ref','expected_state','expected_lane','selection_reason'],properties:{work_ref:{type:'string'},expected_state:{type:'string'},expected_lane:{type:'string'},selection_reason:{type:'string'},repository:{type:['string','null']}},additionalProperties:false}}},additionalProperties:false},async handler(args,ctx){const r=await executeCorrelatedCommand('orchestration.horizon_checkpoint',args||{},input=>createPostgresOrchestrationRunService({db:ctx?.db}).checkpointHorizon(input),{statusForFailure:statusForOrchestrationRunError,defaultError:'ORCHESTRATION_HORIZON_ERROR',defaultMessage:'orchestration.horizon_checkpoint failed',flattenDetails:true,db:ctx?.db});return r.body;}};