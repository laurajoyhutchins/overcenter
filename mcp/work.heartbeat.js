import { executeCorrelatedCommand } from 'lib/orchestration-journal.js';
import { createPostgresWorkLeaseService, statusForWorkLeaseError } from 'lib/work-leases.js';
export const access='admin';
const evidence={type:'array',maxItems:50,items:{type:'object',required:['kind','ref'],properties:{kind:{type:'string'},ref:{type:'string'}},additionalProperties:false}};
export default {name:'work.heartbeat',description:'Persist bounded progress and extend an active lease/slot while its semantic execution contract and run budget remain valid. Expired leases cannot heartbeat.',inputSchema:{type:'object',required:['lease_token','run_id','idempotency_key'],properties:{lease_token:{type:'string'},run_id:{type:'string'},extend_seconds:{type:'integer',minimum:60,maximum:3600},idempotency_key:{type:'string'},checkpoint:{type:['object','null'],properties:{schema:{type:'string',enum:['work-checkpoint-v1']},phase:{type:'string'},next_action_kind:{type:'string',enum:['continue_research','apply_repository_change','run_materializer','verify_candidate','remediate_candidate','integrate_candidate','recheck_external_condition']},candidate:{type:['object','null']},completed:evidence,evidence,authority_revisions:{type:'array',maxItems:25,items:{type:'object',required:['kind','ref','revision'],properties:{kind:{type:'string'},ref:{type:'string'},revision:{type:'string'}},additionalProperties:false}}},required:['phase','next_action_kind'],additionalProperties:false}},additionalProperties:false},async handler(args,ctx){const response=await executeCorrelatedCommand('work.heartbeat',args||{},input=>createPostgresWorkLeaseService({db:ctx?.db}).heartbeat(input),{statusForFailure:statusForWorkLeaseError,defaultError:'WORK_HEARTBEAT_ERROR',defaultMessage:'work.heartbeat failed',flattenDetails:true,db:ctx?.db});return response.body;}};