import { executeCorrelatedCommand } from 'lib/orchestration-journal.js';
import { createPostgresTargetAwareOrchestrationRunService, statusForTargetAwareOrchestrationError } from 'lib/orchestration-run-target-runtime.js';
export const access='admin';
export default {name:'orchestration.horizon_resolve',description:'Revalidate the current durable run horizon without claiming or reprioritizing work. Untargeted runs retain the advisory Linear horizon. Targeted runs reread project graph authority and evaluate their immutable project horizon target; if no production project graph reader is bound, resolution fails closed.',inputSchema:{type:'object',required:['run_id'],properties:{run_id:{type:'string'}},additionalProperties:false},async handler(args,ctx){const r=await executeCorrelatedCommand('orchestration.horizon_resolve',args||{},input=>createPostgresTargetAwareOrchestrationRunService({db:ctx?.db}).resolveHorizon(input),{statusForFailure:statusForTargetAwareOrchestrationError,defaultError:'ORCHESTRATION_HORIZON_ERROR',defaultMessage:'orchestration.horizon_resolve failed',flattenDetails:true,db:ctx?.db});return r.body;}};