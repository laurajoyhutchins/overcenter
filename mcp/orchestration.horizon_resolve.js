import { executeCorrelatedCommand } from 'lib/orchestration-journal.js'; import { createPostgresOrchestrationRunService,statusForOrchestrationRunError } from 'lib/orchestration-runs.js'; export const access='admin'; export default {name:'orchestration.horizon_resolve',description:'Revalidate the latest durable advisory horizon against current Linear execution semantics without claiming or reprioritizing work.',inputSchema:{type:'object',required:['run_id'],properties:{run_id:{type:'string'}},additionalProperties:false},async handler(args,ctx){const r=await executeCorrelatedCommand('orchestration.horizon_resolve',args||{},input=>createPostgresOrchestrationRunService({db:ctx?.db}).resolveHorizon(input),{statusForFailure:statusForOrchestrationRunError,defaultError:'ORCHESTRATION_HORIZON_ERROR',defaultMessage:'orchestration.horizon_resolve failed',flattenDetails:true,db:ctx?.db});return r.body;}};