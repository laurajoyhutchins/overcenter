import { executeCorrelatedCommand } from 'lib/orchestration-journal.js'; import { createPostgresOrchestrationRunService,statusForOrchestrationRunError } from 'lib/orchestration-runs.js'; export const access='admin'; export default {name:'orchestration.finish',description:'Persist a machine-readable run handoff after all owned leases have been truthfully settled. This does not change work authority.',inputSchema:{type:'object',required:['run_id','disposition'],properties:{run_id:{type:'string'},disposition:{type:'string',enum:['completed','clean-stop','blocked','failed','no-work']},last_work_ref:{type:['string','null']},last_gate:{type:['string','null']},stop_reason:{type:['string','null']}},additionalProperties:false},async handler(args,ctx){const r=await executeCorrelatedCommand('orchestration.finish',args||{},input=>createPostgresOrchestrationRunService({db:ctx?.db}).finish(input),{statusForFailure:statusForOrchestrationRunError,defaultError:'ORCHESTRATION_FINISH_ERROR',defaultMessage:'orchestration.finish failed',flattenDetails:true,db:ctx?.db});return r.body;}};