import test from 'node:test';import assert from 'node:assert/strict';import {projectTransitionAuthoritativeEffectConfirmationFor} from '../lib/project-transition-authoritative-effect.js';
const SHA={authority:'1'.repeat(40),candidate:'2'.repeat(40),merge:'3'.repeat(40),development:'4'.repeat(40)};
function fixture(overrides={}){const calls=[];const service=projectTransitionAuthoritativeEffectConfirmationFor({async readLeaseRef(runId){calls.push(['readLeaseRef',runId]);return'lease-1';},executionAuthority:{async require(input){calls.push(['require',input]);return{subject:'project_transition',lease_ref:'lease-1',run_id:'run-1',repository:'laurajoyhutchins/overcenter',project_ref:'github:laurajoyhutchins/overcenter',transition_id:'transition-1',transition_definition_fingerprint:'f'.repeat(64),authority:{kind:'github',repository:'laurajoyhutchins/overcenter',revision:SHA.authority}};}},async deriveWorkspace(){return{repository:'laurajoyhutchins/overcenter',branch:'work/transition-1-abc',authority_revision:SHA.authority};},async resolveBranchRoles(){return{development_branch:'dev'};},async readPullRequests(input){calls.push(['readPullRequests',input]);return[{number:599,state:'closed',merged_at:'2026-09-06T00:49:52Z',merge_commit_sha:SHA.merge,head:{sha:SHA.candidate,ref:'work/transition-1-abc'},base:{ref:'dev'}}];},async readBranchHead(input){calls.push(['readBranchHead',input]);return SHA.development;},async compareCommits(input){calls.push(['compareCommits',input]);return{status:'ahead',behind_by:0};},...overrides});return{service,calls};}
const execution_result={disposition:'completed',evidence:[{kind:'candidate_revision',ref:`github:laurajoyhutchins/overcenter@${SHA.candidate}`},{kind:'verification',ref:'exact-revision-tests:passed'}]};
test('confirms already-integrated exact candidate from fresh GitHub ancestry readback',async()=>{const{service,calls}=fixture();const result=await service.confirm({run_id:'run-1',target:{project_ref:'github:laurajoyhutchins/overcenter',horizon:{kind:'transition',ref:'transition-1'}},execution_result});assert.equal(result.confirmed,true);assert.ok(calls.some(([kind,input])=>kind==='compareCommits'&&input.base===SHA.merge&&input.head===SHA.development));});
test('candidate-only evidence remains unconfirmed',async()=>{const{service}=fixture({async readPullRequests(){return[{number:599,state:'open',merged_at:null,merge_commit_sha:null,head:{sha:SHA.candidate,ref:'work/transition-1-abc'},base:{ref:'dev'}}];}});assert.deepEqual(await service.confirm({run_id:'run-1',target:{project_ref:'github:laurajoyhutchins/overcenter',horizon:{kind:'transition',ref:'transition-1'}},execution_result}),{confirmed:false,reason:'authoritative_effect_not_observed'});});
test('merged candidate outside current development authority remains unconfirmed',async()=>{const{service}=fixture({async compareCommits(){return{status:'diverged',behind_by:1};}});assert.deepEqual(await service.confirm({run_id:'run-1',target:{project_ref:'github:laurajoyhutchins/overcenter',horizon:{kind:'transition',ref:'transition-1'}},execution_result}),{confirmed:false,reason:'authoritative_effect_not_in_development'});});